# 更新日志 / Changelog

本文件记录 **CalaPlayerSrcmBuilder**（CLI + 桌面 GUI）的功能变化。
版本号同时出现在：CLI 横幅与 `-Version`、GUI 右上角版本胶囊（来自 `/api/health`）、
`build_report.json` / `out_patch\work\manifest.json` 的 `version` 字段。

---

## v1.1.0 — 2026-09-26

### 修复：新手引导只在第一次弹了（CP-37c）

* 症状：每次打开 GUI 都自动弹一次新手引导。
* 两个原因：① 页面来自 `http://127.0.0.1:<每次随机的端口>/`，WebView2 的 localStorage 按 origin 分桶 ⇒ 前端存不住（这条上一轮已改用 `%LOCALAPPDATA%\CalaPlayerSrcmBuilder\prefs.json` + `/api/prefs`）；② **本轮真凶**：打包自检为了造出真首次运行，会先清掉 `cala-onboarded`、跑完再还原用户原设置 —— 而用户原本就没有任何设置时，这个还原会把整个 `prefs.json` **删掉**，于是下一次启动又弹。
* 修法：① 引导**一显示就写标记**（不是等用户走完/跳过才写；想再看点「引导」按钮）；② `gui/prefs.py` 新增 `CALA_PREFS_DIR` 覆盖，打包自检把它指向临时目录 ⇒ **再也碰不到用户的真实设置文件**。
* 判据：G4 跑完（它含首次运行必须自己弹、第二次不再弹的断言）之后，用户的 `prefs.json` 必须**原样还在**。

## v1.1.0 — 2026-09-26

向后兼容的功能新增（**只追加、绝不替换任何原生条目**的承诺不变）。

### 新增：画质路线 B —— 背景保持 2560×1440（不再降采样）

* 背景贴图的目标画布从 1920×1080 提升到 **2560×1440**（仍为 `PF_DXT1`/BC1，12 级 mip）。
  2560×1440 的源图从此**不再缩放**；更小的图按原策略 cover/contain 到该画布。
* 实现方式是**整块重建 cooked 贴图的 `.uexp`**（不是补丁式改几个字节）：写入新的
  `SizeX/SizeY`（两处）、`MipCount`、`DataSize`、全部 mip 负载与每级尺寸记录、尾部结构，
  再把 uasset 里**三个**尺寸耦合字段一起修好：`SerialSize`（`len(uexp) - 4`），
  以及 `retoc to-legacy` 搬到 uasset 尾部的 **Zen BulkDataMap** —— 每级 mip 一条 44 B 记录
  （偏移/大小/bulk 标志）**和它前面的记录计数**。
* **安全性来自两道新判据**：
  * 启动即做 **`uexp` 布局回环**——用原件自己的负载、按同一模型在**同尺寸**下重建，必须与原件
    **逐字节一致**；不通过就拒绝在该尺寸上重建（游戏自带的 4096×2048 图集的布局就与可重建的
    壳不同，它会被这条门挡下，而不是被硬套模型）；
  * A6 现在把重建后从**交付容器里读回**的贴图头解出来核对
    （两处 `SizeX/SizeY`、`SizeZ`、`MipCount`、`PixelFormat`、`DataSize`），
    并**用 CUE4Parse 真的把贴图读一遍**（尺寸/格式/12 级 mip/每级 sha256 都要对）。
    ⚠️ 只改记录表不改计数时，CUE4Parse 会读成"11 级正确 + 第 12 级垃圾"，而所有字节级判据仍然全绿——
    这条 CUE4Parse 判据就是为它加的。
* `MI_<名字>` 的六个精灵标量（`SpriteX/Y`、`SpriteWidth/Height`、`TextureWidth/Height`）
  **随目标画布自动同步**（现在写 2560/1440），否则下拉缩略图的取景框会算错。
* 画质实测（离线，与源图对照）：`mixueer_00044` **36.75 dB**、`bg.jpg` **41.71 dB**
  （1920×1080 时为 35.54 / 41.13 dB —— 提升来自"分辨率不再损失"）。

### 新增：预览图集追加 —— 时间轴与右侧预览块终于显示你的背景（2026-09-26）

> 这是本版最重要的功能补齐，也是"图片进游戏"这条链最后两个不显示的地方。

* **静态做法、不需要常驻注入**：每个新背景的 **250×141 缩略图被追加进游戏自己的预览图集**
  `T_BackgroundPreviews`（4096×2048 / `PF_DXT1` / 13 级 mip，格子 250×141、步长 252×143、
  自带 165 格占用 ⇒ 空闲 **序号 165..223 共 59 格**），并把该背景预览材质的
  `SpriteX/SpriteY` 指向那一格。**游戏自己**会把这一格的坐标传给时间轴单元格与右侧预览块
  （两轮只读活进程探针定案：两者都是"接收端"，只收坐标，贴图由游戏材质硬编码指向图集）。
* **像素是等长原地替换**：只改图集 `.uexp` 里"我方格子覆盖的 BC1 块"，**uasset 一个字节不动**
  （所以不需要路线 B 那套 `SerialSize`/`Zen BulkDataMap` 手术）。mip0 的改动**恰好等于格子矩形**，
  **165 个原生缩略图在 mip0 逐字节不变**（A9b 断言 `mip0 changed = 0`）；mips ≥ 1 有 16 格与原生
  邻居共 1 块宽，采用 **P1（原始解码像素 + 我方像素重组再编码）**，代价是邻格边缘 ≤1 像素的重编码
  误差（实测最大通道差 85，**用户肉眼两次判"看不出来"**）。
* **上限 50 → 59**：图集只有 59 个空闲格，因此背景默认上限改成 59（超过要 `-Force`，
  且超出的背景缩略图不会显示）。
* **新增判据 A9 / A9b / A10**：A9 = 差异 BC1 块集合必须 ⊆ 我方格子块集合，并用 CUE4Parse 从容器
  读回图集（尺寸/格式/13 级/每级 sha256 与我们所写一致）；A9b = 原生 165 格采样矩形内的像素差
  （mip0 必须 0，其余报数与最大通道差）；A10 = 容器里读回我方 MI，必须是**原生形态**
  （`SourceTexture` = 图集 + 6 标量 = 格子坐标/250/141/4096/2048）。回归 **26 → 29 场景**。
* **新增 `-NoAtlas` 诊断开关**：回到旧形态（预览材质指向 2560×1440 整图），此时时间轴与
  右侧预览块会退回显示原生图集第 0 格。
* ⚠️ **代价（已定量、并接受）**：下拉列表里的小缩略图**锐度降低约 30%**（真机换补丁前后对比：
  `PSNR = 26.4 dB`、Laplacian 锐度 1715 → 1196）。原因 = 该缩略图只从图集那一格采样，
  格子 250 texel / 显示 146 px ⇒ 采样足迹 1.7 texel/px ⇒ GPU 的 mip 选择落在 mip0 与 mip1 的
  混合上（而 2K 整图路径的足迹是 17.5 texel/px，主要由 mip4 ≈1:1 出图）。**肉眼无感**，
  用户 2026-09-26 拍板接受，作为换取时间轴/预览块完整显示的合理妥协。
* 容器体积相应变大（每张 2560×1440 背景约 2.46 MB）。`-Fit contain` 与宽高比策略不变，
  只是画布随之变成 2560×1440。

### 新增：背景缩略图（下拉条目）修复

* 背景条目的缩略图不是读软路径，而是读 `DA_Backgrounds` 那一行的 **`@30` 硬引用**指向的
  预览材质（`UMaterialInstanceConstant`，父材质 `MMI_BackgroundSelector`）。
* 现在**每个新背景都会自动生成一个自己的预览材质** `MI_<名字>`：
  `SourceTexture` 指向你的贴图、精灵参数设为整幅目标画布，并把它挂到该行的 `@30` 上。
  下拉里的缩略图从此显示你自己的图，而不是游戏原版那一格。
* 每一步都有离线判据（新的 **A8 门**：从容器里读回 `@30`，必须解析成我们自己的材质，
  且该材质的 `SourceTexture` 指向我们的贴图）；`DA_Backgrounds` 的 ImportMap **只在末尾追加**
  （老索引逐条不变，已逐条验证）。
* 诊断开关 `-NoThumb`：万一想回到旧行为（缩略图保持原版、不动 ImportMap），加这个开关即可。

### 新增：宽高比约束（防拉伸）

* 游戏用固定精灵采样背景，**非宽屏素材在游戏内会被拉伸**。现在：
  * **宽 > 高** 一律放行（含 1233×725、3440×1440 等非 16:9 宽屏），日志会提示"不是 16:9"；
  * **竖屏 / 正方形** 默认**拒绝打包**，并给出中文提示（自行裁剪，或用 `-Fit contain`）；
  * 显式 `-Fit contain` 时，竖屏/方图会**居中补 (18,18,22) 黑边**到 2560×1440 后放行。
* 提示同时出现在 CLI 日志与 GUI 的实时日志面板里（同一条日志流）。

### 其他

* 新增开发者文档章节：缩略图 MI 机制、宽高比约束、路线 B 的 `uexp` 重建与判据、A0~A8 判据。
* 回归测试 21 → **29 场景**（新增：宽屏放行 / 方图拦截 / 竖图拦截 / 缩略图通道 / `-NoThumb`）。

### ⚠️ 已知问题（2026-09-26 记录）

* **时间轴单元格 / 编辑器右侧 Background 预览块不显示新增背景** —— 游戏原生蓝图不响应这两个位置
  （连游戏自带背景也不显示）。下拉缩略图、主菜单封面、游戏画面背景本体不受影响。
  运行时补救方案（R2）已废弃；现行路线 = 静态追加预览图集，只读评估见
  [`docs/CP35_ATLAS_APPEND_ASSESSMENT.md`](docs/CP35_ATLAS_APPEND_ASSESSMENT.md)。
* **`-Combined` 不能在上一轮**已有背景**的基础上再加背景**：`da-patch bgref`/`addname` 在
  "已追加过行的 `DA_Backgrounds`"上会让 uexp 多出 34 B 而行计数不变，L3 的 trailer 门会拒绝构建
  （**失败是安全的**：游戏目录零写入、上一轮状态自动还原）。绕行 = 换个新 `out_patch` 做全新构建，
  把上一轮素材按同名文件再放一遍。详见 `docs/DEVELOPER_GUIDE.md` §11。
  **2026-09-26 用户拍板：先记账、不修**（等图集路线定了再一起处理）；建议修法 =
  `-Combined` 时把 DA 基线改用**原生 DA**，carried 行 + 新行统一走字节级 `append_bg_rows`。

---

### 修复：`-Combined` 可以在已有成果上继续累积了（CP-37b）

* 之前的 `-Combined` 在"上一轮已经追加过行"的成果上会失败：`da-patch bgref`/`addname` 走 UAssetAPI
  重新序列化，在一张**已追加**的表上会让 uexp 多出一行（+34 B）而行数不变，被 L3 的尾部门拒绝。
* 现在累积模式把**基线换回游戏原生表**，上一轮的行（含它们的显示名/软路径/预览 MI 身份，从
  `manifest.json` 读回）由**字节级 `append_bg_rows` / `append_audio_rows` 重新追加**，`bgref` 只在
  原生基线上跑。⇒ 可以在已有成果上继续加背景/音频，不再报错。
* 新增回归 **T29**（`-Combined` 在上一轮成果上再加背景）专门守这条；连带修掉"本 run 没有背景素材时
  预览图集没被打包进容器"和一处 A9 详情字符串的空集合崩溃。
* 背景默认上限 = **59**（图集空闲格数）；超限需 `-Force`。GUI 侧也补上了 `-NoAtlas` 开关与中英文案。

## v1.0.0 — 2026-09-23

* 一站式打包：`srcm\{bg,BGM,Sound,Ambient}` → 同名补丁容器 `CalaPlayer-Windows_P.{pak,ucas,utoc}`。
* 背景：克隆原生 1920×1080 壳 + 自研 BC1 编码（自带 `QUALITY: PSNR/MAE/清晰度比` 证据）。
* 音频：克隆最小壳做**流式 PCM** SoundWave，负载 = 原 WAV 逐字节不改；MP3 用 ffmpeg 自动转码。
* `DA_Backgrounds` / `DA_BGM` / `DA_Ambient` / `DA_Sounds` **只追加**新行，绝不替换原生条目。
* 部署安全：只在 `Content\Paks` 放同名补丁容器，装前把已装容器移到 `_prev_container\`，
  `uninstall.ps1` 一键回到安装前状态；原生容器永不写入。
* 离线判据 A0~A7 + `-DryRun`；桌面 GUI（Vue 3 + FastAPI + PyWebView，中英双语、九步引导）。
